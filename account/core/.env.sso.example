# SSO Configuration for Account Folder

# Main script URL (where users login)
MAIN_SCRIPT_URL=https://dewdropskin.com

# Session configuration for SSO (iframe support)
# IMPORTANT: These settings allow cookies to work in iframe context
SESSION_DRIVER=file
SESSION_LIFETIME=525600
SESSION_EXPIRE_ON_CLOSE=false

# For SSO to work in iframe, cookies must allow cross-site requests
# Set same_site to 'none' and secure to true
SESSION_SAME_SITE=none
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_PARTITIONED_COOKIE=true

# Session domain (leave empty or set to your domain)
SESSION_DOMAIN=

# HTTPS is required when same_site=none
# Make sure your site uses HTTPS
